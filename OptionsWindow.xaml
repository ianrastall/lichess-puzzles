<Window x:Class="Lichess_Puzzles.OptionsWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="Options" Height="420" Width="480"
Background="{DynamicResource BackgroundBrush}"
Foreground="{DynamicResource TextBrush}"
WindowStartupLocation="CenterOwner"
ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Board theme" FontSize="16" FontWeight="SemiBold"/>
            <ComboBox x:Name="BoardThemeCombo" Margin="0,8,0,0" Height="32"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Move notation" FontSize="16" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <RadioButton x:Name="SanSymbolsRadio" Content="Symbols (figurines)" GroupName="SanMode" Margin="0,0,14,0"/>
                <RadioButton x:Name="SanLettersRadio" Content="Letters" GroupName="SanMode"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Database export" FontSize="16" FontWeight="SemiBold"/>
            <TextBlock Text="Exports every puzzle from the local database. Files can be very large." 
                       FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       Margin="0,6,0,10"/>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnExportPgn" Content="Export to PGN" Width="130" Click="BtnExportPgn_Click" Margin="0,0,10,0"/>
                <Button x:Name="BtnExportEpd" Content="Export to EPD" Width="130" Click="BtnExportEpd_Click"/>
            </StackPanel>
            <ProgressBar x:Name="ExportProgressBar" Height="10" Margin="0,12,0,0" Minimum="0" Maximum="100" Visibility="Collapsed"/>
            <TextBlock x:Name="ExportStatusText" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
            <Button x:Name="BtnCancelExport" Content="Cancel export" Width="130" Margin="0,10,0,0"
                    Visibility="Collapsed" Click="BtnCancelExport_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnSave" Content="Save" Width="90" Click="BtnSave_Click" Margin="0,0,10,0"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="90" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
